<%- include('layout/header') %>


<div class="container">
  <div class="row my-4">
    <div class="col-lg-12">
        <% if (message) { %>
            <div class="alert alert-dismissible fade show alert-<%= message.type %>" role="alert">
                <button class="btn-close" type="button" data-bs-dismiss="alert" aria-label="Close"></button>
                <strong><%= message.message %></strong>
            </div>
        <% } %>
        <div class="table-responsive">
            <% if (users && users.length > 0) { %>
                <table class="table table-bordered table-striped text-center my-3">
                    <thead class="table-dark">
                        <tr>
                            <th>Manga Name</th>
                            <th>Manga Chapters</th>
                            <th>Finished</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach((row, index) => { %>
                            <tr class="align-middle">
                                <td><%= index %></td>
                                <td><img src="<%= row.image %>" width="50" class="img-thumbnail"></td>
                                <td><%= row.Manga %></td>
                                <td><%= row.Chapters %></td>
                                <td><%= row.Finished %></td>
                                <td>
                                    <a href="/edit/<%= row._id %>" class="btn btn-success btn-sm"><i class="fas fa-edit"></i> Edit</a>
                                    <a href="/delete/<%= row._id %>" class="btn btn-danger btn-sm"><i class="fas fa-trash"></i> Delete</a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <h1 class="text-center text-secondary mt-5">No Manga found in diretory</h1>
            <% } %>
        </div>
    </div>
  </div>
</div>


<%- include('layout/footer') %>